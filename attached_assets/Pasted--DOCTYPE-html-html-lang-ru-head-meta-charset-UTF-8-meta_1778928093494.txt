<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>Pink Sakura</title>

<style>

*{
    margin:0;
    padding:0;
    box-sizing:border-box;
}

body{
    min-height:100vh;
    overflow-x:hidden;

    font-family:'Segoe UI', sans-serif;
    color:white;

    /* ВСТАВЬ СВОЕ ФОТО */
    background:
        url('ТВОЕ_ФОТО.jpg');

    background-size:cover;
    background-position:center;
    background-repeat:no-repeat;
    background-attachment:fixed;

    position:relative;
}

/* НЕЖНЫЙ РОЗОВЫЙ СВЕТ */
body::before{
    content:"";

    position:fixed;
    inset:0;

    background:

        radial-gradient(
            circle at top left,
            rgba(255, 220, 235, 0.22),
            transparent 28%
        ),

        radial-gradient(
            circle at bottom right,
            rgba(255, 182, 210, 0.16),
            transparent 32%
        ),

        linear-gradient(
            rgba(255, 210, 230, 0.05),
            rgba(255, 210, 230, 0.08)
        );

    pointer-events:none;
}

/* ДЛЯ ДЛИННОГО САЙТА */
section{
    min-height:100vh;

    display:flex;
    align-items:center;
    justify-content:center;

    padding:80px 10%;

    position:relative;
    z-index:2;
}

/* КАРТОЧКА */
.card{
    width:min(760px,100%);

    background:rgba(255,255,255,0.06);

    border:1px solid rgba(255,255,255,0.12);

    border-radius:30px;

    padding:60px;

    backdrop-filter:blur(3px);

    box-shadow:
        0 10px 40px rgba(0,0,0,0.10);
}

/* ТЕКСТ */
h1{
    font-size:clamp(55px,8vw,95px);

    margin-bottom:20px;

    color:#fff8fb;

    font-weight:600;

    text-shadow:
        0 2px 10px rgba(0,0,0,0.25);
}

p{
    font-size:19px;

    line-height:1.9;

    color:white;

    text-shadow:
        0 2px 8px rgba(0,0,0,0.30);
}

/* КНОПКА */
button{
    margin-top:35px;

    padding:15px 34px;

    border:none;
    border-radius:999px;

    background:rgba(255,255,255,0.14);

    color:white;

    font-size:16px;

    cursor:pointer;

    transition:0.3s;
}

button:hover{
    transform:translateY(-3px);

    background:rgba(255,255,255,0.22);
}

</style>
</head>

<body>

<section>

    <div class="card">

        <h1>Dreamy Sakura</h1>

        <p>
            Нежный розовый aesthetic фон,
            где фотография остаётся чёткой и красивой,
            а поверх появляется мягкое cinematic свечение.
        </p>

        <button>Explore</button>

    </div>

</section>

</body>
</html>