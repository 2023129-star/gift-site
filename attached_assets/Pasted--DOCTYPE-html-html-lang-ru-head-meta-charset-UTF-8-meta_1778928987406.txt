<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>Dreamy Pink Website</title>

<style>

*{
    margin:0;
    padding:0;
    box-sizing:border-box;
}

html{
    scroll-behavior:smooth;
}

body{
    font-family:'Segoe UI', sans-serif;
    color:white;
    overflow-x:hidden;

    /* ТВОЯ ФОТОГРАФИЯ */
    background:
        url('flowers.jpg') center/cover fixed no-repeat;

    position:relative;
}

/* НЕЖНЫЙ РОЗОВЫЙ СЛОЙ */
body::before{
    content:"";

    position:fixed;
    inset:0;

    background:

        radial-gradient(
            circle at top left,
            rgba(255,240,245,0.30),
            transparent 35%
        ),

        radial-gradient(
            circle at bottom right,
            rgba(255,210,225,0.18),
            transparent 40%
        ),

        linear-gradient(
            rgba(255,255,255,0.03),
            rgba(255,230,240,0.06)
        );

    pointer-events:none;
    z-index:0;
}

/* СЕКЦИИ ДЛЯ ДЛИННОГО САЙТА */
section{
    min-height:100vh;

    display:flex;
    align-items:center;
    justify-content:center;

    padding:100px 10%;

    position:relative;
    z-index:2;
}

/* БЛОКИ */
.card{
    width:min(850px,100%);

    background:rgba(255,255,255,0.08);

    border:1px solid rgba(255,255,255,0.16);

    border-radius:32px;

    padding:60px;

    backdrop-filter:blur(5px);

    box-shadow:
        0 10px 40px rgba(0,0,0,0.08);
}

/* ЗАГОЛОВКИ */
h1{
    font-size:clamp(55px,8vw,95px);

    margin-bottom:20px;

    color:#fffafd;

    font-weight:600;

    text-shadow:
        0 2px 10px rgba(0,0,0,0.12);
}

/* ТЕКСТ */
p{
    font-size:19px;

    line-height:1.9;

    color:rgba(255,255,255,0.96);

    text-shadow:
        0 2px 8px rgba(0,0,0,0.10);
}

/* КНОПКА */
button{
    margin-top:35px;

    padding:15px 34px;

    border:none;
    border-radius:999px;

    background:rgba(255,255,255,0.18);

    color:white;

    font-size:16px;

    cursor:pointer;

    transition:0.3s;
}

button:hover{
    transform:translateY(-3px);

    background:rgba(255,255,255,0.28);
}

/* МОБИЛКИ */
@media(max-width:768px){

    section{
        padding:70px 7%;
    }

    .card{
        padding:35px;
    }

    p{
        font-size:16px;
    }

}

</style>
</head>

<body>

<!-- ПЕРВАЯ СЕКЦИЯ -->
<section>

    <div class="card">

        <h1>Soft Bloom</h1>

        <p>
            Нежный pastel aesthetic сайт
            с полностью видимой фотографией,
            мягким розовым свечением
            и cinematic атмосферой.
        </p>

        <button>Explore</button>

    </div>

</section>

<!-- ВТОРАЯ СЕКЦИЯ -->
<section>

    <div class="card">

        <h1>Dreamy Style</h1>

        <p>
            Фон остаётся одним цельным изображением
            на всём длинном сайте,
            поэтому дизайн выглядит дорогим
            и очень плавным.
        </p>

    </div>

</section>

<!-- ТРЕТЬЯ СЕКЦИЯ -->
<section>

    <div class="card">

        <h1>Pink Atmosphere</h1>

        <p>
            Благодаря fixed background
            появляется красивый эффект глубины
            при скролле страницы.
        </p>

    </div>

</section>

</body>
</html>